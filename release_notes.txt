Bugfix release.

Fixed:
- Multiline values (lines ending with backslash) are now properly joined instead of being truncated at the first line. This was breaking PEM keys and SQL queries stored in .env files.
- Parser rewritten to handle backslash continuation correctly.

Added:
- Test for multiline value parsing.

Tests: 15 passing.
