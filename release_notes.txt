Bugfix release.

Fixed:
- Key ordering in comparison output is now deterministic. Keys are returned in the order they appear in the file, not in random set order. Comparison results no longer jump around between runs.

Added:
- Test for deterministic key ordering in comparison output.

Tests: 21 passing.
