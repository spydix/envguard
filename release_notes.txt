Bugfix release.

Fixed:
- Key comparison is now case-sensitive. API_KEY and api_key are treated as two separate keys, not duplicates. This was causing false-positive duplicate warnings in projects that use mixed casing conventions.

Added:
- Test for case-sensitive key parsing.

Tests: 14 passing.
