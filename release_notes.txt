## What's Fixed

- `--fix` now preserves inline comments (e.g. `KEY=value # comment`) instead of dropping them during sort/deduplicate.
- `--fix` now preserves standalone comment lines (`# ...`) instead of removing them from the file.
- `--fix` now strips the `export ` prefix so that `export KEY=value` is normalized to `KEY=value` in the fixed output.

## Tests

74 tests passing.
